export type ScreenDefinition = {
  code: string;
  title: string;
  area: string;
  batch: string;
  description: string;
};

export type DemoRow = {
  id: string;
  description: string;
  status: string;
  owner: string;
  control: string;
};
